namespace Core
{
    // This interface extends the IStudentCommandHandler interface and inherits its methods.
    public interface IVolumeCommandHandler : IStudentCommandHandler
    {
        // This interface does not introduce any new methods or properties.
        // It inherits the methods from IStudentCommandHandler.
    }
}
